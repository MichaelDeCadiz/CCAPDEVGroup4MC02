<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Lab Reservation System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap JS Bundle (includes Popper for tooltips) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/css/style.css">
<style>
    #lab-container {
        width: 80%;
        margin-left: auto;
        margin-right: auto;
        background-color: rgb(240,240,240);
        border: 1px solid gray;
        border-radius: 20px;
        margin-top: 20px;
    }
    #login-container {
        width: 400px;
        background-color: rgb(240,240,240);
        border: 1px solid gray;
        border-radius: 20px;
        margin-top: 20px;
    }
    #reg-container {
        width: 400px;
        background-color: rgb(240,240,240);
        border: 1px solid gray;
        border-radius: 20px;
        margin-top: 20px;
    }
    #dashboard-container {
      width: 80%;
      margin-left: auto;
      margin-right: auto;
      background-color: rgb(240,240,240);
      border: 1px solid gray;
      border-radius: 20px;
      margin-top: 20px;
    }
    #profile-container {
      width: 80%;
      margin-left: auto;
      margin-right: auto;
      background-color: rgb(240,240,240);
      border: 1px solid gray;
      border-radius: 20px;
      margin-top: 20px;
    }
</style>
</head>
<body>
    <nav class="navbar bg-dark border-bottom border-body sticky-top bg-body-tertiary" data-bs-theme="dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Lab Reservation System</a>
            {{#if session}}
            <div id="yes-session">
                <ul class="nav justify-content-end" id="yes-session">
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/viewlabs">View Labs</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/profile">Profile</a>
                    </li>
                    <li class="nav-item">
                        <form action="/logout" method="POST">
                            <a onclick="this.parentNode.submit();" class="nav-link">Log Out</a>
                        </form>
                    </li>
                </ul>
            </div> 
            {{else}}
            <div id="no-session">
                <ul class="nav justify-content-end" id="no-session">
                    <li class="nav-item">
                        <a class="nav-link" href="/register">Register</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                </ul>
            </div>
            {{/if}}
        </div>
    </nav>
    {{{body}}}
</body>
</html>
